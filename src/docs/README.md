git clone https://github.com/your-repo/lap-time-analyzer.git
   cd lap-time-analyzer

python -m venv venv
   source venv/bin/activate  # Windows: venv\Scripts\activate

pip install -r requirements.txt

python src/main.py;